services:
  integraciones-client:
    image: integraciones-client:development
    container_name: integraciones-client-development
    user: "1001:1001"
    ports:
      - "3003:3000"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - coco-network

networks:
  coco-network:
    external: true