import type { Actions } from './$types';

export async function load() {
	// Los datos se cargan en el cliente para una navegación más rápida
	return {};
}

export const actions = {} satisfies Actions;
